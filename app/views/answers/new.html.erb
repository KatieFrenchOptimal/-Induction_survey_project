<div class="wrap">
  <nav role="navigation">
    <div class="header-icon"></div>
    
    <div id="menuToggle">
      <input type="checkbox" />
      <span></span>
      <span></span>
      <span></span>
      <ul id="menu">
        <%= link_to welcome_index_path do %>
          <li>Home</li>
        <% end %>
        <%= link_to new_study_path do %>
          <li>New study</li>
        <% end %>
        <%= link_to studies_path do %>
          <li>View studies</li>
        <% end %>        
      </ul>
    </div>
  </nav>
  <%
=begin%>
 <h1 class="med-header"><%= @question.question %></h1> 
<%
=end%>
  <h1 class="welcome-header"><%= @study.study_name %></h1>
  <div class="study-wave5"> <%= image_tag  "wave5.svg" %></div>
  <div class="section colour-section">
    <h2><%= @question.question %></h2>
    <%= form_with model: @answer, url: study_question_answers_path(@study, @question), local: true do |form| %>
      <%= form.hidden_field :participant_id %>
      
      <% @question.answers.each do |answer| %>
          <% if answer.errors.any? %>
            <div id="error_explanation">
              <h2>
                <%= pluralize(answer.errors.count, "error") %> prohibited
                this answer from being saved:
              </h2>
              <ul>
                <% answer.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
        <% end %>

      <%= form.text_area :text, class: 'textfield' %><br>
      <div class="submit-btn"><%= form.submit%></div>
    <% end %>
  </div>
  
</div>